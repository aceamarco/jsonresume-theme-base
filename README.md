# JSON Resume Theme Base
*This is not a repo for an npm module! It is a repo of boilerplate code for a JSON Resume Theme!*

# How to use this repo
*Fork(or copy) its contents into ~another~ repo and build from there!*